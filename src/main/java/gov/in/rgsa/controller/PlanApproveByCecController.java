package gov.in.rgsa.controller;

import org.springframework.stereotype.Controller;

@Controller
public class PlanApproveByCecController {
	
	/*
	 * @Autowired private PlanAllocationService planAllocationService;
	 * 
	 * 
	 * public static final String APPROVE_PLAN_BY_CEC = "ApprovePlanByCec";
	 * 
	 * @RequestMapping(value = "ApprovePlanByCec", method = RequestMethod.GET)
	 * private String planForCec(@ModelAttribute("PLAN_ALLOCATION") BasicInfoModel
	 * basicInfoModel, Model model ,RedirectAttributes redirectAttributes) {
	 * //model.addAttribute("PLAN_ALLOCATION_LIST",
	 * allocationService.getPlanComponents()); Boolean
	 * boolean1=planAllocationService.approveCecPlan();
	 * 
	 * if(boolean1== true) { redirectAttributes.addFlashAttribute(Message.
	 * SUCCESS_KEY,"PLAN APPROVED SUCCESSFULLY"); } else {
	 * redirectAttributes.addFlashAttribute(Message.
	 * SUCCESS_KEY,"ERROR OCCURE WHILE APPROVING PLAN"); } return null; }
	 */
}
